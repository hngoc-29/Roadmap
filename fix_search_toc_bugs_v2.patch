diff --git a/lib/presentation/screens/viewer/widgets/toc_drawer.dart b/lib/presentation/screens/viewer/widgets/toc_drawer.dart
index e4beb90..b2fe5c4 100644
--- a/lib/presentation/screens/viewer/widgets/toc_drawer.dart
+++ b/lib/presentation/screens/viewer/widgets/toc_drawer.dart
@@ -22,7 +22,7 @@ class TocDrawer extends StatelessWidget {
       if (b is HeadingBlock) {
         headings.add(_TocEntry(
           index: i,
-          level: b.level,
+          level: b.level.index + 1,
           text:  b.runs.map((r) => r.text).join(),
         ));
       }
diff --git a/lib/presentation/widgets/document_search_bar.dart b/lib/presentation/widgets/document_search_bar.dart
index 329949e..5306300 100644
--- a/lib/presentation/widgets/document_search_bar.dart
+++ b/lib/presentation/widgets/document_search_bar.dart
@@ -110,6 +110,8 @@ class _DocumentSearchBarState extends ConsumerState<DocumentSearchBar>
           onClose:      _close,
           onNext:       () => _navigate(true),
           onPrev:       () => _navigate(false),
+          onToggleCase: () => ref.read(searchNotifierProvider.notifier).toggleCaseSensitive(),
+          onToggleWord: () => ref.read(searchNotifierProvider.notifier).toggleWholeWord(),
         ),
       ),
     );
@@ -126,6 +128,8 @@ class _SearchBarContent extends StatelessWidget {
   final VoidCallback onClose;
   final VoidCallback onNext;
   final VoidCallback onPrev;
+  final VoidCallback onToggleCase;
+  final VoidCallback onToggleWord;
 
   const _SearchBarContent({
     required this.controller,
@@ -135,6 +139,8 @@ class _SearchBarContent extends StatelessWidget {
     required this.onClose,
     required this.onNext,
     required this.onPrev,
+    required this.onToggleCase,
+    required this.onToggleWord,
   });
 
   @override
@@ -221,8 +227,7 @@ class _SearchBarContent extends StatelessWidget {
               label:   'Aa',
               active:  searchState.caseSensitive,
               tooltip: 'Phân biệt hoa/thường',
-              onTap:   () => ref.read(searchNotifierProvider.notifier)
-                  .toggleCaseSensitive(),
+              onTap:   onToggleCase,
             ),
 
             // ── W toggle (whole word) ─────────────────────────────────────
@@ -230,8 +235,7 @@ class _SearchBarContent extends StatelessWidget {
               label:   'W',
               active:  searchState.wholeWord,
               tooltip: 'Toàn từ',
-              onTap:   () => ref.read(searchNotifierProvider.notifier)
-                  .toggleWholeWord(),
+              onTap:   onToggleWord,
             ),
 
             // ── Navigation buttons ────────────────────────────────────────
